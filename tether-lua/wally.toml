[package]
name = "datagrout/tether"
version = "0.2.1"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
description = "DataGrout connector for Roblox — logic cells, LLM inference, and 118+ tools in one ModuleScript"
license = "MIT"
authors = ["DataGrout <hello@datagrout.ai>"]
homepage = "https://github.com/datagrout/tether"

[dependencies]
# No external dependencies — single ModuleScript
